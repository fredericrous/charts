---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: claimspolicies.security.homelab.io
spec:
  group: security.homelab.io
  names:
    kind: ClaimsPolicy
    listKind: ClaimsPolicyList
    plural: claimspolicies
    shortNames:
    - cpol
    singular: claimspolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.resolvedName
      name: Policy
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClaimsPolicy is the Schema for the claimspolicies API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClaimsPolicySpec defines a claims policy for Authelia OIDC
            properties:
              accessToken:
                description: AccessToken lists claims to include in access tokens
                items:
                  type: string
                type: array
              customClaims:
                description: |-
                  CustomClaims lists UserAttribute names to include as custom claims
                  Each must exist as a UserAttribute in the SAME namespace
                items:
                  type: string
                type: array
              customScope:
                description: CustomScope defines a custom OIDC scope associated with
                  this policy
                properties:
                  claims:
                    description: Claims to include when this scope is requested
                    items:
                      type: string
                    minItems: 1
                    type: array
                  scopeName:
                    description: ScopeName is the scope name. Defaults to policy name
                      if not set.
                    type: string
                required:
                - claims
                type: object
              idToken:
                description: IDToken lists claims to include in ID tokens
                items:
                  type: string
                type: array
              introspection:
                description: Introspection lists claims to include in introspection
                  responses
                items:
                  type: string
                type: array
              policyName:
                description: |-
                  PolicyName is the name referenced by OIDCClient.claimsPolicy
                  If not specified, derived from metadata.name (hyphens → underscores)
                type: string
              userinfo:
                description: Userinfo lists claims to include in userinfo responses
                items:
                  type: string
                type: array
            type: object
          status:
            description: ClaimsPolicyStatus defines the observed state
            properties:
              conditions:
                description: Conditions represent the current state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncedAt:
                description: LastSyncedAt is the timestamp of the last successful
                  sync
                format: date-time
                type: string
              ready:
                description: Ready indicates if the policy is valid and synced
                type: boolean
              resolvedName:
                description: ResolvedName is the final Authelia policy name
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
