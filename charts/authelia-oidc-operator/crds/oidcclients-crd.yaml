---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: oidcclients.security.homelab.io
spec:
  group: security.homelab.io
  names:
    kind: OIDCClient
    listKind: OIDCClientList
    plural: oidcclients
    shortNames:
    - oidc
    singular: oidcclient
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clientId
      name: Client ID
      type: string
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OIDCClient is the Schema for the oidcclients API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OIDCClientSpec defines the desired state of OIDCClient
            properties:
              audience:
                description: Audience is the list of allowed audiences for this client
                items:
                  type: string
                type: array
              authorizationPolicy:
                description: AuthorizationPolicy for the client
                enum:
                - one_factor
                - two_factor
                type: string
              clientId:
                description: ClientID is the unique identifier for the OIDC client
                type: string
              clientName:
                description: ClientName is the display name for the client
                type: string
              consentMode:
                description: ConsentMode determines how user consent is handled
                enum:
                - auto
                - explicit
                - implicit
                - pre-configured
                type: string
              generateSecret:
                default: true
                description: GenerateSecret indicates if a secret should be generated
                type: boolean
              grantTypes:
                default:
                - authorization_code
                description: GrantTypes are the allowed grant types
                items:
                  type: string
                type: array
              pkceChallengeMethod:
                default: S256
                description: PKCEChallengeMethod is the PKCE challenge method
                enum:
                - S256
                - plain
                type: string
              preconfiguredConsentDuration:
                description: PreconfiguredConsentDuration is the duration for pre-configured
                  consent
                type: string
              public:
                default: false
                description: Public indicates if this is a public client (no client
                  secret required)
                type: boolean
              redirectUris:
                description: RedirectURIs are the allowed redirect URIs
                items:
                  type: string
                minItems: 1
                type: array
              requirePkce:
                default: false
                description: RequirePKCE indicates if PKCE is required for authorization
                  code flow
                type: boolean
              responseModes:
                default:
                - form_post
                - query
                - fragment
                description: ResponseModes are the allowed response modes
                items:
                  type: string
                type: array
              responseTypes:
                default:
                - code
                description: ResponseTypes are the allowed response types
                items:
                  type: string
                type: array
              scopes:
                default:
                - openid
                - profile
                - email
                - groups
                description: Scopes are the allowed scopes
                items:
                  type: string
                type: array
              secretRef:
                description: SecretRef references an existing secret containing the
                  client secret
                properties:
                  key:
                    default: client_secret
                    description: Key in the secret containing the client secret
                    type: string
                  name:
                    description: Name of the secret
                    type: string
                  namespace:
                    description: Namespace of the secret (defaults to the OIDCClient
                      namespace)
                    type: string
                required:
                - name
                type: object
              sectorIdentifierUri:
                description: SectorIdentifierURI for pairwise subject identifier
                type: string
              tokenEndpointAuthMethod:
                default: client_secret_basic
                description: TokenEndpointAuthMethod is the authentication method
                  for the token endpoint
                enum:
                - client_secret_basic
                - client_secret_post
                - client_secret_jwt
                - private_key_jwt
                - none
                type: string
              userinfoSignedResponseAlg:
                default: none
                description: UserinfoSignedResponseAlg is the algorithm for signing
                  userinfo responses
                type: string
            required:
            - clientId
            - redirectUris
            type: object
          status:
            description: OIDCClientStatus defines the observed state of OIDCClient
            properties:
              conditions:
                description: Conditions represent the current state of the OIDCClient
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncedAt:
                description: LastSyncedAt is the timestamp of the last successful
                  sync
                format: date-time
                type: string
              ready:
                description: Ready indicates if the client is ready
                type: boolean
              secretName:
                description: SecretName is the name of the generated/referenced secret
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
